import SwiftUI

extension Color {
    static let snatchdBlack = Color.black
    static let snatchdDarkGray = Color(red: 0.12, green: 0.12, blue: 0.12)
    static let snatchdWhite = Color(red: 0.95, green: 0.95, blue: 0.95)
    static let glassBackground = Color.white.opacity(0.1)
}
